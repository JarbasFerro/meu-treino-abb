rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /workout_profiles/{profileId}/app_data/workout_data {
      allow read, write: if request.auth != null
        && profileId == 'jarbas';
    }
  }
}
